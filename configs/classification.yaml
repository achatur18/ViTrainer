# config.yaml

# Dataset path
dataset:
    train_path: "/Users/abhaychaturvedi/Documents/Work/ViTrainer/data/catdog/training_set/training_set"
    test_path: "/Users/abhaychaturvedi/Documents/Work/ViTrainer/data/catdog/test_set/test_set"

# Model parameters
model:
    architecture: "vgg16"
    optimizer: "Adam"
    learning_rate: 0.001
    loss_function: "categorical_crossentropy"
    metrics: ["accuracy"]
    decay_learning_rate: False

# Training parameters
training:
    epochs: 10
    batch_size: 32

# Training parameters
testing:
    batch_size: 32

output:
    directory: "output"
    weights_name: "classification_model.pth"